import Head from 'next/head'

const Header = ({children}) => {
    return (
        <>
            <Head></Head>
            <div>
                Header
            </div>
        </>
    )
}

export default Header